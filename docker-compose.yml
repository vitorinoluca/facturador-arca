services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: facturador
      POSTGRES_PASSWORD: facturador_dev
      POSTGRES_DB: facturador_arca
    ports:
      - '5434:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
